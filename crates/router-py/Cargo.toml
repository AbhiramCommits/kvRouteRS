[package]
name = "router-py"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PyO3 bindings for the kvRouteRS router (stub)"

[lib]
name = "router_py"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py39"] }

[build-dependencies]
pyo3-build-config = "0.23"
