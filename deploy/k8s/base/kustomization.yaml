apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kvrouters

resources:
  - namespace.yaml
  - rbac.yaml
  - router-deployment.yaml
  - router-service.yaml
  - router-hpa.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: kvrouters
    includeSelectors: true
